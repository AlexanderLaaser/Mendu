// /** @type {import('next').NextConfig} */
// const nextConfig = {
//   output: 'export',
//   // Andere Konfigurationen, falls nötig
// };

// module.exports = nextConfig;

// next.config.js
module.exports = {
  images: {
    unoptimized: true,
  },
  output: 'export',
};
